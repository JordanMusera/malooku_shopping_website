/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['fakestoreapi.com','res.cloudinary.com'], // Allow images from this domain
      },
};

export default nextConfig;
